{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "colors": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "default": "#000000",
          "description": "Text color"
        }
      }
    },
    "font": {
      "type": "string",
      "default": "Arial, sans-serif",
      "description": "Font family for all event text"
    },
    "textSize": {
      "type": "object",
      "properties": {
        "current": {
          "type": "number",
          "default": 48,
          "minimum": 8,
          "maximum": 300,
          "description": "Font size (px) for the current event title"
        },
        "upcoming": {
          "type": "number",
          "default": 32,
          "minimum": 8,
          "maximum": 300,
          "description": "Font size (px) for upcoming event titles"
        }
      }
    },
    "position": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 100,
          "description": "Horizontal position of the container (% from left)"
        },
        "y": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 100,
          "description": "Vertical position of the container (% from top)"
        }
      }
    },
    "alignment": {
      "type": "string",
      "enum": ["left", "right"],
      "default": "left",
      "description": "Text alignment"
    },
    "maxTextWidth": {
      "type": "number",
      "default": 1800,
      "minimum": 50,
      "description": "Maximum rendered width (px) of a text line before it is truncated with …"
    },
    "countVisibleEvents": {
      "type": "number",
      "default": 4,
      "minimum": 1,
      "maximum": 10,
      "description": "Total number of events to display (including current)"
    }
  },
  "additionalProperties": false
}
