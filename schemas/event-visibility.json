{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "description": "Store public/private state for each event",
  "additionalProperties": {
    "type": "boolean",
    "description": "True if event is public (visible), false if private (hidden)"
  },
  "default": {}
}
